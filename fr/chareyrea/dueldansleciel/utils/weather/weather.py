import debug

class Weather:

    def render(self):
        debug.warn("rendering weather", "Weather wasn't initialized")

    def tickUpdate(self):
        debug.warn("rendering weather", "Weather wasn't initialized")
